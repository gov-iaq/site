# استنساخ الموقع لجمعيةٍ أخرى

دليلٌ تنفيذيّ من الصفر إلى موقعٍ حيٍّ على النطاق الرسميّ **مباشرةً** بلا نطاقٍ مؤقّت.
كل رقمٍ فيه مُتحقَّقٌ من المستودع في 2026-08-02، لا مقدَّر.

---

## ١) القرار المعماريّ: انسخ المستودع، ولا تُحوّله إلى متعدّد الجمعيات

**لجمعيةٍ واحدةٍ إضافية الآن: `fork`.**

السبب بالأرقام:

| ما هو | العدد | هل تُصلحه التعدّدية؟ |
|---|---|---|
| اسم الجمعية مكتوبًا حرفيًّا في `src/` | **113** مرّة في **17** ملفًّا | جزئيًّا — 72 منها في ملفّات بياناتٍ تُكتب من جديد لكل جمعية على أي حال |
| النطاق `iaq.org.sa` في `src/` | **39** مرّة في **7** ملفّات | نعم، برمزٍ واحد |
| وثائق PDF خاصّة بالجمعية | **24** ملفًّا، **18 م.ب** | لا |
| صور (مجلس، أخبار، تراخيص، شركاء) | **4 م.ب** | لا |
| ملفّات SQL | **10** (منها `setup.sql` بـ988 سطرًا) | لا |
| أعمدة انتماء في المخطّط (`association_id`/`tenant_id`/`site_id`) | **0** | هي جوهر العمل، وتعني إعادة كتابة المخطّط والدوالّ وكل استعلامات اللوحة |

المخطّط يفترض جمعيةً واحدة افتراضًا عميقًا: `settings` مفتاحها `key` وحده فلا يختلف
`contact_email` بين موقعين، و`content_overrides` فريدةٌ على `(page,path,op,attr,part)`
و`page` سبيكةٌ مثل `index` فالتصادم تامّ، و`page_views` بلا معرّف موقع، و`is_admin()`
تفحص وجود البريد في `admins` فقط فأيّ مديرٍ يكتب على محتوى الجمعيتين. والتخزين
و`auth.users` لا يُفصلان بمخطّط.

فالنتيجة: **مشروع Supabase منفصل حتمًا، وعامل Cloudflare منفصل حتمًا** — والتعدّدية
ستوفّر نحو أربعين سلسلةً نصّية وتُبقي القاعدة والاستضافة والأصول مضاعفةً.

**متى يتغيّر القرار:** عند الجمعية **الثالثة**، أو أبكر إن صانَ الجميعَ فريقٌ واحد —
لأن إصلاحًا واحدًا في `head.html` يصير تطبيقًا يدويًّا × N مع خطر انحرافٍ صامت.

---

## ٢) الخطوات بالترتيب

**قبل البدء:** Python مع `pillow` · Node و npm · حساب Cloudflare · حساب Supabase ·
السيطرة على سِجِلّ النطاق (`.org.sa` عبر SaudiNIC أو مُسجِّل معتمد) · بيانات الجمعية
موثّقة (ترخيص، عنوان، أعضاء، وثائق PDF، شعار PNG شفّاف) · بريد المدير الأوّل.

### ١. مستودعٌ بلا تاريخ الجمعية القديمة

انسخ شجرة العمل بلا `node_modules` و`site`، ثم احذف `.git` وابدأ من جديد:

```bash
git init && git add -A && git commit -m "أساس الموقع"
```

**التحقّق:** `git log --oneline` سطرٌ واحد.

> الاستنساخ بالتاريخ يحمل 18 م.ب من وثائق الجمعية القديمة وبياناتها الشخصية إلى
> مستودع جمعيةٍ أخرى إلى الأبد.

### ٢. أفرِغ الأصول القديمة قبل أي بناء

احذف محتويات `src/static/img/{board,team,news,licenses,partners}` و`src/static/files/`
و`src/static/favicon.png` و`src/static/og-image.png`، واحذف `supabase/seed-content.sql`.

```bash
find src/static -name "*.pdf" | wc -l   # يجب أن يكون 0
```

### ٣. املأ ملفّات البيانات في `src/data/`

`contact.json` · `pages.json` · `about.json` · `assembly-members.json` ·
`board-members.json` · `team-members.json` · `partners.json` · `files.json` ·
`news.json` · `legal.json` · `disclosure.json`

في `pages.json`: `site_url` الجديد، و**أبقِ `production: false`**.
في `contact.json`: الترخيص والمدينة والعنوانان والهاتفان والبريد والساعات وروابط
التواصل و`donate_url` و`admin_login_url` بهاشٍ **جديد**.

**التحقّق:**
```bash
git grep -n "حاضنة الجمعيات\|iaq\.org\.sa\|2253\|بريدة" -- src/data
```

> `disclosure.json` فيه إقراراتٌ نظاميّة عن أعضاء مجلسٍ بأعيانهم («لا توجد علاقة
> عائلية بين…»). نقلها كما هي = نشر إقرارٍ كاذبٍ باسم الجمعية الجديدة. يُعاد إقراره
> من مختصّ الجمعية الجديدة.

### ٤. بدّل السلاسل المبثوثة في الشيفرة والقوالب

هذه **لا رموزَ لها**، فحارس الرموز في البنّاء لن ينبّه عليها والبناء سينجح صامتًا:

`src/templates/head.html` (الشعار base64، `og:site_name`، روابط الصورة المطلقة،
JSON-LD) · `src/templates/header.html` · `src/templates/footer.html` (`aria-label`،
`alt`، `brand-ar`، `brand-en`، نصّ التذييل) · `src/content/index.main.html` ·
`src/templates/about-tabs.html` · `src/static/404.html` · `src/panel/design.html`
(الاسم، والألوان الافتراضية، وقوالب الردّ) · `src/build.py` (كتلة `real["brand"]`) ·
`src/gen_seed.py`

**التحقّق:**
```bash
git grep -c "حاضنة الجمعيات" -- src   # يجب أن يكون 0
```

### ٥. بدّل هاش اللوحة في **ستّة** مواضع بإيداعٍ واحد

```bash
git mv src/static/iaq-cp-9f4b21.html src/static/<اسم-جديد>.html
```
ثم `src/data/contact.json` (مفتاح `admin_login_url`) و**أربعة** مواضع في `src/build.py`
(منها `PANEL_PATHS` الذي يُبني منه `robots.txt`).

**التحقّق:** `git grep -n "iaq-cp-9f4b21" .` = صفر.

> تعديل `contact.json` وحده ⇒ البوّابة تُحوّل إلى صفحةٍ غير موجودة فتُقفل اللوحة.
> إعادة التسمية وحدها ⇒ اللوحة تُبنى باسمٍ لا يوافقها.
> نسيان `PANEL_PATHS` ⇒ `robots.txt` يمنع مسارين وهميّين ويترك مسارَي اللوحة مفتوحين للزواحف.

### ٦. الشعار والأيقونات

شعار PNG شفّاف يُدرَج base64 في **سبع نسخٍ داخل خمسة ملفّات**: `head.html`،
`header.html`، `footer.html`، `panel/design.html`، و`404.html` (ثلاث نسخ).
ومعه `favicon.png` (64×64) و`og-image.png` (1200×630).

### ٧. الصور والوثائق الجديدة

```bash
python src/import_people_photos.py board
python src/import_people_photos.py team
python src/import_news_images.py
```
ثم ارفع ملفّات PDF إلى `src/static/files/…` بنفس المسارات المكتوبة في `files.json`.

### ٨. مشروع Supabase الجديد

في SQL Editor، بالترتيب:

```
supabase/setup.sql        (988 سطرًا — يشمل v1..v7)
supabase/schema-v8.sql    (231 سطرًا — الإحصاءات؛ غير مُدرَجٍ في setup)
```

بلا الثاني تفشل شاشتا «إحصاءات الزوّار» و«سجلّ العمل».
وقبل التشغيل: بدّل نصّ الشرائح الثلاث وراجع عناصر القائمة الـ27 داخل `setup.sql`.

**التحقّق:** `select count(*) from public.page_views;` و`select * from v_audit_daily limit 1;`
ينجحان، وتقرير ذيل كل ملفّ يظهر الأعداد.

### ٩. المدير الأوّل — بهذا الترتيب

1. Supabase → Authentication → Users → أضِف البريد وكلمة المرور.
2. **ثم** أدرِج صفًّا في `public.admins` بنفس البريد ودور `admin`.

الصفّ وحده لا يكفي (الدخول بكلمة مرور عبر `/auth/v1/token`)، وغيابه يُسقط اللوحة
برسالة «بريدك غير مُسجَّل».

### ١٠. اربط الموقع بالقاعدة

في `src/data/supabase.json`: `url` و`publishable_key` الجديدان،
و**احذف `anon_key_legacy`**.

**التحقّق بعد البناء:**
```bash
grep -rl "xolcbzmvqudgenenewug" site | wc -l   # يجب أن يكون 0
```

### ١١. حمّل المحتوى إلى القاعدة

```bash
python src/gen_seed.py
```
يُعيد توليد `supabase/seed-content.sql` من ملفّات بياناتك الجديدة. **قبل تشغيله:**
اجعل مفتاح `lists_from_db` بقيمة `false` حتى تتأكّد من اكتمال الصفوف — مع `true`
وقاعدةٍ ناقصة تُفرَّغ قوائم الأعضاء والشركاء على الموقع فعلًا.

### ١٢. ابنِ وافحص محليًّا

```bash
python src/build.py
```
يفشل بصوتٍ عالٍ على أي رمزٍ لم يُستبدل. ثم `npm run preview`، وافتح صفحة اللوحة،
وادخل، وتأكّد من شريط الحالة «متصل — N خبرًا · N طلبًا…».

**التحقّق:**
```bash
grep -rl "حاضنة\|iaq\.org\.sa\|iaq-cp-9f4b21\|xolcbzmvqudgenenewug" site
```
لا شيء.

### ١٣. العامل الجديد

في `wrangler.toml` بدّل `name = "iaq"` باسم الجمعية الجديدة، ثم:

```bash
npx wrangler whoami
npm run ship
```

**التحقّق:** `https://<name>.<subdomain>.workers.dev` يفتح الموقع الجديد،
و`/robots.txt` فيه `Disallow: /` (لأن `production` ما زال `false`).

### ١٤. أضِف النطاق إلى Cloudflare

Add a site → النطاق مجرّدًا → Free → **راجع السجلّات المستوردة بندًا بندًا:**
`MX` و`SPF` و`DKIM` و`TXT`. صوّر سجلّات المزوّد الحاليّ قبل هذا.

### ١٥. بدّل خوادم الأسماء عند المُسجِّل

انتظر حتى تصير حالة النطاق `Active`. **لا تُكمل قبلها.**

### ١٦. لحظة التحويل

Workers & Pages → العامل → Settings → Domains & Routes → Add → **Custom Domain** →
النطاق. Cloudflare يُنشئ السجلّ ويُصدر شهادة TLS تلقائيًّا.

**التحقّق:** `curl -I https://<domain>` يعيد 200 والقفل مُغلق.

### ١٧. `www`

سجلّ `CNAME www → <domain>` مُشغَّل الوكالة، وقاعدة توجيه 301 من نوع Dynamic:

```
concat("https://<domain>", http.request.uri.path)
```
مع Preserve query string.

### ١٨. افتح الفهرسة

في `src/data/pages.json` اجعل `production: true` ثم `npm run ship`.

**التحقّق:** `robots.txt` فيه `Allow: /` وسطر `Sitemap:`، و`sitemap.xml` فيه
**18** رابطًا كلها بالنطاق الجديد، ولا `noindex` في مصدر أي صفحة.

### ١٩. أغلق العنوان الثاني

أضِف `workers_dev = false` إلى `wrangler.toml` ثم انشر — وإلّا بقي
`<name>.workers.dev` يخدم نفس الموقع ويعلن `Allow: /` بعد الخطوة ١٨.

### ٢٠. ما بعد الإطلاق

Search Console (تحقّق بسجلّ DNS) وتقديم `sitemap.xml` · أرسل نموذج «تواصل معنا»
وتأكّد من ظهوره في شاشة «الطلبات» · افتح «إحصاءات الزوّار» للتأكّد من تطبيق
`schema-v8`.

---

## ٣) تغيير الهوية البصرية

### من اللوحة، بلا إعادة بناء

- **الألوان والحواف** — يشتقّ 27 رمزًا جذريًّا و7 عائلاتٍ مُنطَقة.
- **الشعار** — يستبدل كل `img.brand-mark` في الترويسة والتذييل ويضبط شعار الخلفية.
- **الخطّ** — خمس عائلاتٍ مستضافة محليًّا، والتخطيط لا يتأثّر.
- **أيقونة التبويب (favicon)**.
- **السلايدر الرئيسيّ** — النصوص والروابط والأيقونات وصورة الخلفية وتعتيمها.
- **نمط شريط الشركاء** — تلقائيّ · يدويّ · تلاشٍ — وسرعته.
- **أقسام الرئيسية** — إظهارها وترتيبها.
- **بيانات التواصل والروابط الاجتماعية** — والفارغُ يُخفى تلقائيًّا.
- **كل نصوص المحتوى** عبر طبقة النصوص، و**القوائم**، و**الأخبار والوثائق والأعضاء والشركاء**.
- **أكواد مخصّصة** (CSS/JS).

### يلزمه تعديل مصدرٍ وإعادة بناء

- **إضافة منصّة تواصلٍ جديدة** خارج السبع المدعومة (تحتاج أيقونة SVG في البنّاء).
- **إضافة خطٍّ سادس** إلى القائمة.
- **إنشاء صفحةٍ جديدة** أو تغيير عنوانها أو وصفها.
- **الشعار المُدرَج base64** في القوالب (اللوحة تستبدله وقت التشغيل، لكن المصدر يبقى قديمًا).
- **`og-image`** وبيانات JSON-LD.
- **بنية الأقسام** نفسها لا محتواها.
- **صفحة 404** — لها نسخةٌ مستقلّة من النمط (423 قاعدة، منها 57 لا وجود لها في القالب).

---

## ٤) مصائد النسخ كما هو — مرتّبةً بالخطورة

1. **`wrangler.toml: name = "iaq"`** — أوّل `npm run ship` من النسخة الجديدة
   **يستبدل موقع الجمعية القديمة الحيّ**. الاسم نفسه = العامل نفسه، ولا `account_id`
   ولا `routes` في الملفّ يحمي من ذلك. افحص `name` و`npx wrangler whoami` قبل أوّل نشر.

2. **`donate_url: "https://iaq.sa"`** — زرّ «ادعمنا» في موقع الجمعية الجديدة يقود إلى
   متجر تبرّعات الجمعية القديمة. **تحويل أموالٍ إلى جهةٍ أخرى**، لا خطأ تجميليّ.

3. **مشروع Supabase مشترك** — لا عمود انتماء في المخطّط كلّه (صفر)، فالتصادم تامّ في
   `settings` و`content_overrides` و`people` و`partners` و`news` و`documents`
   و`page_views`. و`is_admin()` تمنح أيّ مديرٍ الكتابة على محتوى الجمعيتين.

4. **`anon_key_legacy`** — مرجع المشروع القديم مبثوثٌ **داخل حمولة JWT بترميز base64**،
   فبحثٌ نصّيٌّ عن معرّف المشروع **لا يطابقه**. والبنّاء يستعمله احتياطًا: لو فرغ
   `publishable_key` سهوًا اتّصل الموقع الجديد بقاعدة الجمعية القديمة صامتًا.

5. **هاش اللوحة في ستّة مواضع** — وسقوط `contact.json` يُعيد البناء إلى الهاش القديم
   في ثلاثة مواضع **بلا أي خطأ**.

6. **الأصول التي لا تُنظَّف تصير نشرًا لمستندات جمعيةٍ أخرى** — صور تراخيصٍ تحمل الرقم
   الوطنيّ الموحّد ورقم الترخيص، وصور أخبارٍ فيها وجوه مدرّبين وشعارات داعمين، وصور
   مجلسٍ **الشعار محروقٌ فيها في البِكسل**، وأسماء تحميل وثائقٍ تنتهي بـ
   « - حاضنة الجمعيات.pdf» يراها الزائر.

7. **`supabase/seed-content.sql` القديم** — تشغيله على المشروع الجديد يُدرج **33 شخصًا**
   بأسمائهم الحقيقية وهواتفهم وبُردهم، وأخبارًا ووثائق وشركاء. **حادثةُ بياناتٍ شخصية**
   لا خطأ بناء. الحلّ الوحيد: `python src/gen_seed.py` بعد استبدال ملفّات البيانات.

8. **`setup.sql` نفسه ليس محيَّدًا** — ثلاث شرائح سلايدر بنصّ الجمعية القديمة، و27 عنصر
   قائمة. وطبقة التشغيل تستبدل الشرائح المبنيّة بصفوف القاعدة فعلًا على الصفحة الرئيسة.

9. **`disclosure.json`** — إقراراتٌ نظاميّة عن أعضاءٍ بأعيانهم.

10. **بريد المدير ونطاقه** — في `contact.json` و`team-members.json` (ثلاثة مواضع)
    و`placeholder` صفحة الدخول ونموذج `setup.sql`.

11. **ألوان اللوحة الافتراضية** — ثمانية مواضع على تركوازيّ الجمعية القديمة وذهبِها.
    أيّ «حفظ» في شاشة الألوان ينشرها إلى `settings`، فلو ضغط المدير الجديد حفظًا بلا
    تغييرٍ سرى لونُ الجمعية القديمة على الموقع كلّه.

12. **حارس الرموز في البنّاء لا يُنبّه على أيٍّ من هذا** — كلها قيمٌ نصّية لا رموز.

---

## ٥) ربط النطاق الرسميّ مباشرةً

**الفرق عن المرّة السابقة:** الحالة القديمة عاشت على نطاقٍ مؤقّت ثم انتقلت.

- **لا مهرب من عنوان `workers.dev` كسطح اختبارٍ واحد** — العامل يجب أن يوجد قبل أن
  يُربط به نطاق. الاختبار يجري عليه ومحليًّا، ثم يُغلق بـ`workers_dev = false` بعد
  الإطلاق (بندٌ ناقصٌ في `الإطلاق.md`).
- **`canonical` و`sitemap` يشيران إلى النطاق الرسميّ من أوّل بناء**، ولهذا يبقى
  `production: false` طوال مرحلة `workers.dev`: وإلّا صار عنوانٌ مؤقّتٌ يُعلن `Allow: /`
  ويقدّم خريطةً لنطاقٍ لا يخدم الموقع بعد.
- **إن كان للنطاق بريدٌ قائم**، فلحظة التحويل الفعلية هي ربط Custom Domain (١٦) لا
  تبديل خوادم الأسماء (١٥) — بشرط استيراد `MX`/`SPF`/`DKIM` استيرادًا صحيحًا في (١٤).

**الترتيب الذي يمنع الانقطاع وخطأ الشهادة:**
انشر العامل وافحصه (١٣) ← أضِف النطاق واستورد السجلّات (١٤) ← بدّل خوادم الأسماء
وانتظر `Active` (١٥) ← اربط Custom Domain ودع Cloudflare يُصدر TLS (١٦) ←
`www` والتوجيه 301 (١٧) ← `production: true` (١٨) ← أغلق `workers.dev` (١٩).

**علَم `production`** مصدره `src/data/pages.json`، ويحوّل ثلاثة أشياء في بناءٍ واحد:
`robots.txt` من `Disallow: /` إلى `Allow: /` مع سطر الخريطة، وتوليد `sitemap.xml`
بـ18 رابطًا، وإزالة `noindex` من كل صفحة مع بقائه على اللوحة وصفحة الدخول.
والرجوع فوريّ: `false` ثم انشر.

---

## ٦) ما يجب تعديله في `الإطلاق.md` قبل استعماله لجمعيةٍ أخرى

- جدول «الوضع الآن» كلّه خاصٌّ بهذه الجمعية (اسم العامل، والنطاق المؤقّت،
  و`iaq.org.sa`، و`iaq.sa`).
- الخطوة ٣ مكتوبةٌ على عاملٍ اسمه `iaq`، وسطر «يبقى النطاق المؤقّت عاملًا — مفيدٌ
  للاختبار» يجب أن يصير: يبقى مؤقّتًا **ثم يُغلق**.
- صفّ التحقّق يذكر هاش اللوحة القديم.
- قسم «ما لا يحتاج تغييرًا» **مُضلٌّ في نسخةٍ جديدة**: يقول إن Supabase لا شيء فيه
  يُعدَّل وإن `site_url` صحيحٌ أصلًا — وفي النسخة الجديدة الاثنان يتغيّران.
- يُضاف: إنشاء مشروع القاعدة وتشغيل `setup.sql` **ثم** `schema-v8.sql`، وإنشاء
  مستخدم Auth **ثم** صفّ `admins`، ونقل تحذير `MX` إلى خطوة استيراد السجلّات،
  وبند إغلاق `workers.dev`.
- في جدول «إن حدث خطأ» يُضاف سطر: «اللوحة تُحوّل إلى صفحةٍ غير موجودة ← الهاش بُدِّل
  في `contact.json` ولم يُبدَّل اسم الملفّ في `src/static` (أو العكس)».
