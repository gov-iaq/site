# الصيانة الدورية — ما لا تفعله القاعدةُ بنفسها

مشروعُ Supabase على الخطّة المجّانية لا يملك `pg_cron`، فلا مُجدولَ داخل
القاعدة. وهذه مهمّتان تحتاجان يدًا أو مُجدولًا خارجيًّا. لا شيءَ منهما عاجل،
وتأخيرُ شهرٍ لا يضرّ — لكنّ تركَهما سنةً يُكبّر الجدولين بلا فائدة.

---

## ١) تنظيفُ سجلّ العمل — مرّةً كلَّ شهر

سجلُّ العمل (`audit_log`) يحفظ الصفَّ القديمَ كاملًا لكلّ تعديل كي يعمل
التراجع. فينمو بحجم ما يُكتب، ويحمل بياناتٍ شخصيةً من الطلبات.

في **Supabase → SQL Editor**:

```sql
select public.purge_audit(30) as حُذف;
```

`30` = عددُ الأيام التي يُحتفظ بها. وهي المدّةُ التي يعمل فيها زرُّ «التراجع»
في شاشة لوحة التحكّم — فتقصيرُها يُقصّر نافذةَ التراجع، وتطويلُها يُطيل بقاءَ
البيانات الشخصية. ثلاثون يومًا موازنةٌ معقولة.

**من يُشغّلها:** صاحبُ الموقع وحده (`is_owner`). المحرّرُ يُرفَض.

---

## ٢) تنظيفُ سجلّ الزيارات — مرّةً كلَّ سنة

جدولُ `page_views` سطرٌ لكلّ زيارةٍ وتنزيلٍ ونقرة. لا بياناتٍ شخصيةَ فيه
(لا معرّف زائر ولا كوكيز ولا IP) لكنّه ينمو بلا حدّ.

```sql
select public.purge_views(400) as حُذف;
```

`400` يومًا كي يبقى مدى «منذ الإنشاء» في اللوحة سنةً كاملةً وزيادة. وأيُّ
حذفٍ هنا **يُنقص أرقامَ اللوحة للفترة المحذوفة** — فلا تُقصّرها بلا سبب.

---

## ٣) فحصُ صحّة الإحصاء — عند كلّ مراجعة

```sql
select * from public.v_views_health;
```

| العمود | ما يعنيه | ما تفعله إن كان غير صفر |
|---|---|---|
| `capped_days` | أيامٌ بلغت السقف اليوميّ | أرقامُ تلك الأيام حدٌّ لا قياس. إن لم تكن حركةً حقيقيةً فهي تضخيم — راجع الأيام واحذف ما تراه ملفَّقًا |
| `future_rows` | صفوفٌ بتاريخٍ مستقبليّ | أُدرجت قبل ترقية `v11` حين كان تلفيقُ التاريخ ممكنًا. احذفها |
| `today` مقابل `cap` | حركةُ اليوم مقابل السقف | إن قاربت الحركةُ الحقيقيةُ السقفَ فارفعه: عدّل `views_day_cap()` |

ولوحةُ التحكّم تُعلن `capped_days` و`future_rows` بنفسها فوق الرسوم — فلا
حاجةَ إلى تذكّر هذا الفحص، إنّما هو للتفصيل.

**حذفُ الصفوف المستقبلية:**

```sql
delete from public.page_views where day > (now() at time zone 'Asia/Riyadh')::date;
```

**رفعُ السقف اليوميّ** (إن صار الموقعُ يتجاوز ٥٠٠٠ حدثًا في اليوم فعلًا):

```sql
create or replace function public.views_day_cap()
returns int language sql immutable as $$ select 20000 $$;
```

---

## ٤) دورةُ مجلس الإدارة — عند التجديد

تُحدَّث من **لوحة التحكّم → مجلس الإدارة → دورة المجلس المعتمدة**، لا من
ملفّات المشروع. وتسري على صفحة «مجلس الإدارة» عند أوّل تحميلٍ بعد الحفظ، بلا
إعادة بناءٍ ولا نشر.

ولوحةُ التحكّم تُنبّه في صدرها قبل الانتهاء بستّين يومًا، وتُعلن الانتهاءَ
بعده — فلا حاجةَ إلى تذكّر التاريخ.

> ملفُّ `src/data/board-members.json` يبقى مصدرَ **الأعضاء** وقتَ البناء
> ومصدرَ القيمة الافتراضية للدورة. وما يُحفظ في اللوحة يغلبه حقلًا حقلًا.

---

## ٥) جدولةُ المهمّتين آليًّا — اختياريّ

إن أردتَ ألّا تتذكّر، فأرخصُ سبيلٍ **Cloudflare Worker Cron** (المشروعُ
منشورٌ على Cloudflare سلفًا). عاملٌ صغيرٌ منفصلٌ ينادي الدالّتين عبر
PostgREST بمفتاح `service_role`.

> **تنبيه:** مفتاحُ `service_role` يتخطّى كلَّ سياسات الوصول. لا يُوضع في أيّ
> ملفٍّ داخل هذا المستودع ولا في أيّ ملفٍّ يصل المتصفّح — بل في
> **Cloudflare → Workers → Settings → Variables → Encrypt** وحدها.

```
POST https://<PROJECT>.supabase.co/rest/v1/rpc/purge_audit
Headers: apikey: <service_role>, Authorization: Bearer <service_role>
Body:    {"keep_days": 30}
```

وإن لم ترغب في ذلك فتشغيلُ الاستعلامين يدويًّا مرّةً في الشهر ومرّةً في
السنة كافٍ تمامًا.

---

## ما لا يحتاج صيانةً

- **النسخُ الاحتياطي** — Supabase يأخذه يوميًّا على الخطّة المجّانية.
- **الشهادات** — Cloudflare يُجدّدها.
- **الخطوط والصور** — مستضافةٌ في المستودع، لا تنتهي.
- **الجلسات** — تُجدَّد آليًّا قبل انتهائها بخمس دقائق.
