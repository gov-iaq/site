# الإطلاق على النطاق الرسمي — خطوة بخطوة

الوضع الآن:

| | |
|---|---|
| المستضيف | Cloudflare Workers — اسم العامل `iaq` |
| الرابط الحالي | `https://iaq.gov-15b.workers.dev` |
| النطاق المستهدف | `https://iaq.org.sa` |
| نطاق التبرّعات | `iaq.sa` — منفصل تمامًا، لا يُمسّ |
| الفهرسة | **ممنوعة** (`production = false`) |

النشر: `npm run ship` (يبني ثم ينشر) — أو `python src/build.py` ثم `npx wrangler deploy`.

---

## الخطوة ١ — أضِف النطاق إلى Cloudflare

1. **Cloudflare Dashboard → Add a site**
2. اكتب `iaq.org.sa` (بلا `www` وبلا `https://`)
3. اختر الخطة **Free** — تكفي تمامًا لهذا الموقع
4. سيقرأ Cloudflare سجلّاتك الحالية. **راجعها**: إن كان لديك بريدٌ على النطاق
   فتأكّد من وجود سجلّات `MX` و`SPF` و`DKIM` كما هي — وإلا انقطع بريدك
5. في آخر الخطوة يعطيك **عنوانَي خادم أسماء** مثل:
   `xxx.ns.cloudflare.com` و `yyy.ns.cloudflare.com`

> **قبل أن تكمل:** صوّر سجلّاتك الحالية عند مزوّدك الحاليّ. إن حدث خطأ فهذه
> صورتك للرجوع.

---

## الخطوة ٢ — بدّل خوادم الأسماء عند المُسجِّل

نطاقات `.org.sa` تُدار عبر **SaudiNIC** (`nic.sa`) أو عبر المُسجِّل المعتمد
الذي سجّلتَ النطاق من خلاله.

1. ادخل لوحة إدارة النطاق (SaudiNIC أو مُسجِّلك)
2. ابحث عن **خوادم الأسماء / Name Servers / DNS**
3. **احذف** الخوادم الحالية و**ضع** خادمَي Cloudflare
4. احفظ

**المدّة:** من دقائق إلى ٤٨ ساعة. Cloudflare يُرسل بريدًا عند التفعيل، وحالة
النطاق في لوحته تتحوّل من `Pending` إلى **`Active`**.

**لا تُكمل الخطوة ٣ قبل أن تصير `Active`.**

---

## الخطوة ٣ — اربط النطاق بالعامل

1. **Cloudflare → Workers & Pages → `iaq`**
2. **Settings → Domains & Routes → Add → Custom Domain**
3. اكتب `iaq.org.sa` → **Add domain**

Cloudflare ينشئ سجلّ DNS وشهادة TLS تلقائيًّا. بعد دقائق يفتح
`https://iaq.org.sa` على موقعك.

*يبقى `iaq.gov-15b.workers.dev` عاملًا — مفيدٌ للاختبار.*

---

## الخطوة ٤ — توجيه `www` إلى النطاق المجرَّد

بلا هذا يصير للموقع عنوانان بمحتوًى واحد، وهذا يُضعف الفهرسة.

**أ) سجلّ DNS:**
- **DNS → Records → Add record**
- Type `CNAME` · Name `www` · Target `iaq.org.sa` · Proxy **مُشغَّل** (السحابة برتقالية)

**ب) قاعدة التوجيه:**
- **Rules → Redirect Rules → Create rule**
- الاسم: `www to root`
- الشرط: `Hostname` **equals** `www.iaq.org.sa`
- النوع: **Dynamic**
- التعبير: `concat("https://iaq.org.sa", http.request.uri.path)`
- الحالة: **301 (Permanent)**
- ✅ **Preserve query string**

اختبر: `https://www.iaq.org.sa/about.html` يجب أن يقود إلى
`https://iaq.org.sa/about.html`

---

## الخطوة ٥ — افتح الفهرسة

في `src/data/pages.json` بدّل:

```json
"production": false
```

إلى:

```json
"production": true
```

ثم:

```bash
npm run ship
```

هذا **وحده** يُحوّل ثلاثة أشياء معًا:

| | قبل | بعد |
|---|---|---|
| `robots.txt` | `Disallow: /` | `Allow: /` + سطر خريطة الموقع |
| `sitemap.xml` | غير موجود | يُولَّد من قائمة الصفحات (١٨ رابطًا) |
| وسم الصفحات | `noindex` في كل صفحة | يُزال — ويبقى على اللوحة وصفحة الدخول |

---

## الخطوة ٦ — تحقّق بعد الإطلاق

| الفحص | المتوقّع |
|---|---|
| `https://iaq.org.sa` | يفتح الموقع، وقفل الأمان مُغلق |
| `https://iaq.org.sa/robots.txt` | `Allow: /` وسطر `Sitemap:` |
| `https://iaq.org.sa/sitemap.xml` | ١٨ رابطًا كلها بالنطاق الجديد |
| `https://www.iaq.org.sa` | يقود 301 إلى النطاق المجرَّد |
| مصدر أي صفحة | **لا** `noindex`، و`canonical` بالنطاق الجديد |
| `https://iaq.org.sa/iaq-cp-9f4b21.html` | صفحة الدخول تعمل |
| اللوحة | تُحمَّل وتقرأ البيانات |
| نموذج «تواصل معنا» | يُرسل فعلًا (تحقّق من شاشة الطلبات) |

ثم **Google Search Console**: أضِف النطاق (تحقّق بسجلّ DNS من Cloudflare) وقدّم
`https://iaq.org.sa/sitemap.xml`.

---

## ما لا يحتاج تغييرًا

- **Supabase**: الدخول بكلمة مرور مباشرةً (`/auth/v1/token`) ولا يستعمل روابط
  إعادة توجيه، والقراءة تُصرّح بالمفتاح لا بالنطاق. فلا شيء يُعدَّل.
  *(لو أُضيف لاحقًا «نسيت كلمة المرور» فسيلزم إضافة النطاق في
  Authentication → URL Configuration.)*
- **`site_url`** في `pages.json` هو `https://iaq.org.sa` أصلًا، والروابط
  المرجعية مبنيّة عليه.
- **`iaq.sa`** متجر التبرّعات — منفصل، ولا يُمسّ.

---

## إن حدث خطأ

| العَرَض | السبب الغالب |
|---|---|
| النطاق `Pending` أكثر من ٤٨ ساعة | خوادم الأسماء لم تُحفظ عند المُسجِّل — راجعها |
| «Domain is not active» عند الربط بالعامل | لم تصر الحالة `Active` بعد — انتظر |
| خطأ شهادة | انتظر دقائق؛ الشهادة تُصدَر بعد الربط |
| البريد انقطع | سجلّات `MX` لم تُنقل — أضِفها في DNS كما كانت |
| الموقع يفتح على `workers.dev` ولا على النطاق | الربط لم يكتمل: راجع Domains & Routes |

---

## للرجوع عن الإطلاق

اجعل `production = false` ثم `npm run ship` — تعود الفهرسة ممنوعة فورًا.
وحذف النطاق من Domains & Routes يُعيد الموقع إلى `workers.dev` وحده.
