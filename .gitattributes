# توحيد نهايات الأسطر على LF داخل المستودع وفي نسخة العمل
# (يطابق مخرجات build.py ويمنع فروقات وهمية عند إعادة البناء)
* text=auto eol=lf

# ملفات ثنائية — لا تُمسّ نهايات أسطرها
*.png binary
*.pdf binary
*.zip binary
*.ico binary
